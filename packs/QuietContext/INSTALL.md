# Install

After bootstrapping CLAMP Core once with `python3 install_clamp.py`, dry-run this pack with:

```bash
python3 ~/.codex/tools/pack_install.py \
  ~/.codex/packs/QuietContext \
  --dry-run
```

Install:

```bash
python3 ~/.codex/tools/pack_install.py \
  ~/.codex/packs/QuietContext
```
